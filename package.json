{
  "name": "type-inference-ts-project",
  "version": "1.0.0",
  "description": "A project for demonstrating type inference in TypeScript.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^4.5.0",
    "jest": "^28.0.0",
    "@types/jest": "^28.0.0",
    "ts-jest": "^28.0.0"
  }
}
